title: skills
description: A skill corpus for AI code review and the tools it runs on, with what each rule was measured on.
url: https://davd-gzl.github.io
baseurl: /skills
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter_opts:
    disable: true
defaults:
  - scope:
      path: ""
    values:
      layout: default
exclude:
  - Gemfile
  - Gemfile.lock
