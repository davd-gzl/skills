# One reader alone lists what one reading sees

A single strong agent running every stage in sequence reaches the same top of the list as the pipeline at a fifteenth of the calls, verifies a third of it, and never returns what an angle it did not walk would have found.

- One opus agent at xhigh, alone, on a 1,227-line pull request: 12 candidates, 5 confirmed, 3 of them Warnings, nothing wrong, 71 tool calls of the 250 allowed. The pipeline's round on the same code: 58 candidates, 51 confirmed, 7 Warnings, 1,045 calls.
- Its 71 calls: four on the rules, ten on the checkout and the worktrees, nine reading the diff and its neighbours, eleven runs of one probe package on one question, the rest writing. No mutation of a test, no class-by-class catalog walk, no refactor pass, no critic asking what was missing.
- The 46 rows it lacked came from the angles it never named: fifteen missing tests from mutations, twenty Nits and Suggestions from the refactor pass, two Warnings from the catalog's sub-identity class.
- It stopped on its own list with 179 calls unspent: a bound is not what ends a single reader's search.

Source: [gnolang/gno#6187](https://github.com/gnolang/gno/pull/6187), the blind single-agent round's transcript with its `tool_use` blocks counted, against `./scripts/review-retro.py` over the pipeline's round on the same head.

Changes: an angle is a fresh context with one question, never a checklist one reader promises to walk; a cheap first pass is one strong reader, and what it returns is the top of the list, not the list.
