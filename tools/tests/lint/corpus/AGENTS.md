# Fixture root

A rule stated on 2026-01-02 that carries a date inside a rule line, so it fires.
A rule that says stop re-deriving the capability, which the frozen check names.
A rule pointing at *Fixture root*, this file's own heading, which resolves.
A rule pointing at *Nowhere at all*, which no file carries.

- **A bold rule** whose commit is pinned at 0123456789abcdef, which the sha check names.
- A rule saying docker cannot run here without the command beside it.
- A rule with a parenthetical (like this one) that the paren check names.
- A rule with an exempt marker (s) and a numbered one (2) that pass the paren check.
- A rule with an em-dash — inside a bullet head, which is structure and not prose.
- A sentence with an em-dash in prose runs long enough that the dash sits past sixty characters — like this one here.
- A rule pointing at `skills/none.md`, which does not exist in this fixture.
- A rule pointing at *Missing Section* in `skills/a.md`, which has no such heading.
- A rule pointing at *Present heading* in `skills/a.md`, which resolves.
- A rule pointing at *Bold label* in `skills/a.md`, a bold paragraph label there.
- A rule pointing at `skills/a.md` and its *Present heading* on the same line resolves through the file named.
- A sentence that was measured
, and the next line opens on punctuation so the dangling check fires.
- Meeting at 10:30 UTC is a clock the dated check names.
- A rule keeping `a sha 0123456789abcdef inside a code span` where the sha check does not look.
- A link [with a paren in its target](https://example.com/a_(b)) that the cleaner blanks first.

This exact sentence appears in two files of the fixture corpus so the dupe check fires.
This opening clause appears in two files, but the tail of it differs in each one here.
| a table cell (with a paren) | that the paren check skips |

```
A fenced block with an em-dash — and (a paren) and 2026-01-01 that nothing reports.
```

A closing line after the fence, with a **bold** word and no finding.
