# p delta

- Measured on 2026-05-06: the suite takes 4 minutes, a dated line a delta may carry.
- The tree was read at 89abcdef0 for this fact, a sha a delta may carry.
- The recipe is in `scripts/setup-dev.sh`, which this fixture lacks.
- A capability line: the box cannot reach the network, with no command, warns here too.
